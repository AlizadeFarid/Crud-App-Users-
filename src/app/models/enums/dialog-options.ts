import { MessageType } from "./swal-eunms";

export class DialogOptions {
    title: string;
    text: string;
    iconType: MessageType;
    confirmButtonText: string;
    cancelButtonText: string;
    confirmDialogTitle: string;
    confirmDialogHtml: string;
  }
